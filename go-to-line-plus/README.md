# Go To Line (plus!) package
Move the cursor to a relative or specific line in the editor using <kbd>ctrl-g</kbd>.

Usage was meant with [relative-numbers](https://atom.io/packages/relative-numbers).
